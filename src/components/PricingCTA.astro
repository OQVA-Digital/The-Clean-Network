---

const { intro, end, job, price, minimumHours } = Astro.props;

---

<div class="service_cta">
    <p>
        <span set:html={intro}></span>
        {price ? (
            <span>
                Our <strong>{job.toLowerCase()}</strong> start from a minimum of <b>{minimumHours} hours per week</b> from <b>just £{price}+vat per hour</b>.
            </span>
        ) : null} 
        {end ? (
            <strong>{end}</strong>
        ) : (
            <strong>Get in touch with us today for a free quote.</strong>
        )}
    </p>
</div>