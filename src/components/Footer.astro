<footer>
    <div class="ctnr">
        <div class="content">
            <a href="/" class="logo_anchor">
                <img src="/tcn-logo-default.svg" alt="The Clean Network logo" />
            </a>
            
            <p>Our ethos is to provide high standards of cleaning with strong account management to ensure smooth day to day running of your cleaning contract at an affordable price.</p>

            <p class="legal">Copyright © 2023 The Clean Network </p>
        </div>

        <nav>
            <a href="#"><span></span>Home</a>
            <a href="#"><span></span>Services</a>
            <a href="#"><span></span>Sectors</a>
            <a href="#"><span></span>Locations</a>
            <a href="#"><span></span>About</a>
            <a href="#"><span></span>contact_box</a>
        </nav>

        <div class="contact_box">
            <a href="#" class="email">
                <span class="icon"></span>
                office@cleannetwork.co.uk
            </a>
            
            <a href="#" class="tel">
                <span class="icon"></span>
                London: 020 3411 0275
            </a>
            
            <a href="#" class="tel">
                <span class="icon"></span>
                Birmingham: 0121 3927529
            </a>

            <hr>

            <div class="social">
                <a href="#" class="icon instagram" title="Instagram"></a>

                <a href="#" class="icon facebook" title="Facebook"></a>

                <a href="#" class="icon linkedin" title="LinkedIn"></a>
            </div>
        </div>
    </div>
</footer>

<style>
    footer {
        background-color: var(--dark);
        color: var(--light);
        padding: clamp(4rem, 13vw, 8rem) 0;
        font-size: clamp(1rem,3.9vmin,1.7rem);
        line-height: 1.5;
    }

    footer > .ctnr {
        width: var(--mobile-width);
        margin-inline: auto;
        display: flex;
        flex-flow: row wrap;
        gap: clamp(3rem, 10vw, 12rem);
    }

    footer .content {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        flex: 1;
    }

    footer .logo_anchor {
        width: clamp(10rem, 50vw, 25rem);
        display: block;
    }

    footer .content p {
        max-width: 38rem;
    }

    footer .legal {
        font-style: italic;
        color: var(--light-b);
    }

    footer nav {
        display: flex;
        flex-flow: row wrap;
        gap: 1.2rem 2rem;
        align-content: start;
    }

    footer nav a {
        display: flex;
        align-items: center;
        gap:1rem;
    }

    footer nav a span {
        display: block;
        border-bottom: 0.5rem solid var(--grey);
        border-right: 0.5rem solid var(--grey);
        border-top: 0.5rem solid transparent;
        border-left: 0.5rem solid transparent;
        transform: rotate(-45deg);
        transition: border 0.4s ease;
    }

    footer nav a:hover span {
        border-bottom: 0.5rem solid var(--highlight);
        border-right: 0.5rem solid var(--highlight);
        transition: border 0s ease;
    }

    
	@media screen and (min-width: 900px) {

        footer nav {
            flex-direction: column;
        }

        footer .content {
            max-width: 40vw;
        }

        footer .legal {
            margin-top: 2rem;
        }

    }
</style>
