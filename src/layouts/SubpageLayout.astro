---
const { title, description, keywords } = Astro.props;

import BaseHead from "../components/BaseHead.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import BodyGTM from "../components/BodyGTM.astro";

import "../styles/subpages.css";

import { SITE_KEYWORDS } from "../consts";
---

<!DOCTYPE html>
<html lang="en">
    <head>
        <BaseHead title={`${title} ${String(title).includes('Cleaning') ? "" : "Cleaning"} Service · The Clean Network`} description={description} keywords={SITE_KEYWORDS} />
    </head>
    <body>
        <BodyGTM/>
        <Header />
        
        <main>
            <slot />
        </main>

        <Footer />

        <!-- Position:sticky address bar fix -->
        <!-- <div style="position: fixed;"></div> -->
    </body>
</html>
